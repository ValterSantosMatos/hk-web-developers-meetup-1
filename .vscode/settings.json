// Place your settings in this file to overwrite default and user settings.
{
    "files.exclude": {
        "**/.git": true,
        "**/*.js.map": true,
        "app/**/*.js": {
            "when": "$(basename).ts"
        },
        "node_modules/**": true,
        "typings/**": true
    }
}